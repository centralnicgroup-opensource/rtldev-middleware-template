node_modules/
vendor/
build/
dist/
coverage/
reports/
docs/api/
pnpm-lock.yaml

# Generated and owned by semantic-release (@semantic-release/changelog), which
# commits it through @semantic-release/git and therefore never passes it through
# lint-staged. So the copy in git is unformatted, and any human commit touching the
# file would make lint-staged reflow the whole changelog — hundreds of lines of churn
# to edit two. Left alone instead.
HISTORY.md
CHANGELOG.md

# Prettier has no PHP parser; phpcs owns PHP formatting.
*.php
